{
  "scripts": {
    "build:sw": "node scripts/build-sw.mjs",
    "sync:android": "node scripts/build-sw.mjs && node scripts/build-www.mjs && cap sync android",
    "build:apk": "npm run sync:android && cd android && JAVA_HOME=${JAVA_HOME:-/usr/lib/jvm/java-21-openjdk-amd64} ./gradlew assembleRelease",
    "test": "node scripts/test-games.js"
  },
  "dependencies": {
    "puppeteer-core": "^25.1.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
